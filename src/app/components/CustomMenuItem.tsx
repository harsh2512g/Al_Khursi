import React from 'react';
import { Menu } from 'antd';

interface CustomMenuItemProps {
    label: string;
    key: string;
}

const CustomMenuItem: React.FC<CustomMenuItemProps> = ({ label, key }) => {
    return (
        <div>skadbfkjsbd</div>
    );
};

export default CustomMenuItem;